# validator

This module is the former `ph-ee-id-account-validator-impl` repo, merged into
this repo. It holds the GSMA and Mojaloop account validation implementations
used by the identity account mapper.

It is a plain library module: the root application consumes it directly as
`project(':validator')`. It is not published and it is not a runnable app.
